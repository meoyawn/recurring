t_10:
  title: implement env passing
  success_criteria: apps/api can talk to local compose postgres without hardcoding host, port, username, password, etc.

t_7:
  title: implement `/healthz` in api
  deps:
    - t_3
    - t_10

t_8:
  title: "`task web:dev` starts"
  deps:
    - t_7

t_6:
  title: test google auth
  deps:
    - t_8

t_9:
  title: forward W3C `traceparent` and `tracestate` from Solid server requests to Recurring API requests

t_11:
  title: create spike for api static config shape
  success_criteria: project spike markdown
