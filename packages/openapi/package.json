{
  "name": "@recurring/openapi",
  "private": true,
  "type": "module",
  "scripts": {
    "generate:client": "openapi-generator-cli generate -c config/client.yaml",
    "generate:server-stubs": "openapi-generator-cli generate -c config/server-stubs.yaml",
    "tsc": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@openapitools/openapi-generator-cli": "^2.32.0",
    "@responsibleapi/ts": "^1.0.7",
    "@types/bun": "^1.3.13",
    "typescript": "^6.0.3"
  }
}
