[defaults]
inventory = inventories/prod/hosts.ini
roles_path = roles
host_key_checking = False
# Keep controller temp inside the repo (ignored) so restricted sandboxes and CI behave the same.
local_tmp = .ansible/tmp
