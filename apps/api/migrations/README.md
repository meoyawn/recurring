# SQL migrations

Canonical schema migrations for the API live here. Apply with your tool of choice (e.g. `goose`, `atlas`, `migrate`); Terraform, Ansible, and local compose should treat this directory as the source of truth.
